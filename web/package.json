{
  "name": "web",
  "version": "1.0.0",
  "description": "Web app for Dataclick activity",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.14.0"
  }
}
